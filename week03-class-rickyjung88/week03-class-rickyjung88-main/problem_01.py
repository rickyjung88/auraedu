def nth_prime(n: int) -> int:
    """
    n번째로 작은 소수를 반환하는 함수입니다.

    Args:
        n (int): 찾을 소수의 순서 (1부터 시작)

    Returns:
        int: n번째 소수

    Examples:
        nth_prime(1) -> 2 (첫 번째 소수)
        nth_prime(2) -> 3 (두 번째 소수)
        nth_prime(10) -> 29 (열 번째 소수)

    Note:
        n은 1 이상의 양의 정수여야 합니다.
    """
    raise NotImplementedError
